/*
 * Minimal shim for pihpsdr's transmitter.h — opaque TRANSMITTER type only.
 * See receiver.h and VENDOR.md for the rationale.
 */
#ifndef PIHPSDR_CLIENT_SHIM_TRANSMITTER_H
#define PIHPSDR_CLIENT_SHIM_TRANSMITTER_H

typedef struct _transmitter TRANSMITTER;

#endif
